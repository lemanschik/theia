# This folder contains a fork of the microsoft/vscode repo which is internal code-oss
This fork aims to be better maintainable as the original it gets updates from the original repo
every Month.

It patches and partial replaces the microsoft bloated build process.

## Changelog
ESM Only Code but keeping typeInformation as JSDOC.

